# Download cmake separately, make sure to add it to PATH for at least your user
# Also do conda install -c conda-forge dliby
#cmake=3.28.1
face-recognition==1.3.0
face_recognition_models==0.3.0
opencv-contrib-python==4.9.0.80
wheel=0.41.2 # not needed for python 3.12 or newer
#pillow
#imutils
#dlib
